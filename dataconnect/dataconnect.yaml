specVersion: "v1"
serviceId: "meow"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "meow-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
